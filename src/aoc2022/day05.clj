(ns aoc2022.day05
  (:require [clojure.string :as s])
  (:gen-class))

;todo

;(let [[stacks commands] (-> (slurp "resources/input05.txt")
;                            (s/split #"\n\n"))
;      s (s/split-lines stacks)
;      [ids & args] (reverse s)])
;
;
;
;
;(defn part1 [[stacks commands]]
;  (let []))